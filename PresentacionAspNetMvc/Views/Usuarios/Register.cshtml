@model TiendaVirtual.Entidades.IUsuario

@{
    ViewBag.Title = "Register";
    Layout = "~/Views/Shared/_Layout.cshtml";

    //var mostrarModal = (Boolean)HttpContext.Current.Session["mostrarModal"];

}
<section class="col-sm-offset-1 col-sm-10">
    <h2>Register</h2>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h3>
                <span class="glyphicon glyphicon-plus "></span>
                <span class="glyphicon glyphicon-user "></span>
                &nbsp;&nbsp;Registrar Usuario
            </h3>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                @Html.LabelFor(model => model.Nick, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Nick, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Nick, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(model => model.Password, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Password, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Password, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Guardar" class="btn btn-primary" />
                </div>
            </div>
        </div>

        @*<div id="RegisterOk" class="modal fade bs-example-modal-sm" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel">
            <div class="modal-dialog modal-sm" role="document">
                <div class="modal-content">
                    <p>Usuario creado satisfactoriamente</p>
                </div>
            </div>
        </div>*@
    }

    <div>
        @*@Html.ActionLink("Back to List", "Index")*@
        <a href="@Url.Action("Index", "Productos")">
            <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
            Volver a la Tienda
        </a>
    </div>
</section>


@*@section Scripts
{
    @if (mostrarModal)
    {
    <script>
        $('#RegisterOk').modal('show');

        HttpContext.Session["mostrarModal"] = false;

        $('#RegisterOk').on('hidden.bs.modal', function (e) {
            window.location.href = '/..';
        })

    </script>
    }
}*@
